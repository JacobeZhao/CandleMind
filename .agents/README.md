# Project Agents

This directory is version-controlled and reserved for repository-specific
agent definitions, reusable review roles, and workflow metadata shared by all
contributors.

Keep machine-local state, caches, transcripts, temporary worktrees, and secrets
out of this directory. Project contribution rules remain authoritative in
`../AGENTS.md`; files added here should complement those rules rather than
duplicate them.
