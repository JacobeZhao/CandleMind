# 安全策略

## 支持范围

安全修复以默认分支当前版本为准。历史提交、个人分支和未经维护者发布的部署不在
支持范围内。本项目用于研究和模拟执行，当前策略运行时必须保持 paper-only。

## 报告漏洞

请先查看仓库托管平台是否已启用私密安全报告功能；如已启用，优先通过该入口提交。
如未启用，请联系维护者索取非公开报告渠道。不要在公开 Issue、讨论区或聊天群中
披露可利用细节、API Key、数据库或账户信息。

报告应包含受影响版本、复现步骤、影响范围和建议修复方式。维护者确认安全接收渠道
前，不要发送真实凭据或完整生产数据。本仓库不承诺尚未配置的响应时限或私密披露
能力。

## 密钥泄露处置

如密钥曾进入提交、日志、截图或聊天记录，应立即在服务商侧撤销并重新签发，随后
清理运行环境和历史暴露面。仅删除仓库中的字符串不能使已泄露密钥恢复安全。

Binance Key 应遵循最小权限原则，禁用提现权限。当前代码不应执行真实下单、撤单、
杠杆或保证金修改；发现任何交易所写路径应按高优先级漏洞报告。部署服务默认只应
绑定受信任网络，生产环境还需独立配置鉴权、TLS、访问控制和备份保护。
